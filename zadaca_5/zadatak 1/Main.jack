class Main
{
    function void main()
    {
        var Array A;

        let A = Array.new(6);

        let A[0] = 7;
        let A[1] = 5;
        let A[2] = 2;
        let A[3] = 6;
        let A[4] = 10;
        let A[5] = 2;

        do QuickSort.sort(A, 0, 5);

        do QuickSort.printArray(A, 5);

        do A.dispose();

        return;
    }
}